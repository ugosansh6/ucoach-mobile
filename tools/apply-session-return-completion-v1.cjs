const fs = require('fs');
const path = require('path');

const root = path.resolve(__dirname, '..');
const sessionPath = path.join(root, 'app', 'workout', 'session.js');

const raw = fs.readFileSync(sessionPath, 'utf8');
const eol = raw.includes('\r\n') ? '\r\n' : '\n';
let text = raw.replace(/\r\n/g, '\n');

const oldBlock = `                        <Pressable\n                          onPress={() =>\n                            validateBlock(\n                              block.id\n                            )\n                          }\n                          disabled={\n                            !canValidate ||\n                            block.validated\n                          }\n                          style={({ pressed }) => [\n                            styles.validateButton,\n                            block.validated &&\n                              styles.validateButtonDone,\n                            !canValidate &&\n                              !block.validated &&\n                              styles.validateButtonDisabled,\n                            pressed &&\n                              canValidate &&\n                              !block.validated &&\n                              styles.validateButtonPressed,\n                          ]}\n                        >\n                          {block.validated ? (\n                            <>\n                              <Ionicons\n                                name=\"checkmark-circle\"\n                                size={20}\n                                color={\n                                  colors.primaryLight\n                                }\n                              />\n                              <Text\n                                style={styles.validateButtonDoneText}\n                              >\n                                BLOC VALIDÉ\n                              </Text>\n                            </>\n                          ) : (\n                            <Text\n                              style={[\n                                styles.validateButtonText,\n                                !canValidate &&\n                                  styles.validateButtonTextDisabled,\n                              ]}\n                            >\n                              {block.id ===\n                                'warmup' &&\n                                'TERMINER LE WARM-UP'}\n                              {block.id ===\n                                'tabata' &&\n                                'TERMINER LE TABATA'}\n                              {block.id ===\n                                'skill' &&\n                                'TERMINER LE SKILL'}\n                              {block.id ===\n                                'wod' &&\n                                'TERMINER LA SÉANCE'}\n                            </Text>\n                          )}\n                        </Pressable>`;

if (!text.includes(oldBlock)) {
  throw new Error('Bloc du bouton de validation introuvable dans app/workout/session.js');
}

const newBlock = `                        {block.id === 'wod' &&\n                        block.validated &&\n                        workout.status !== 'completed' ? (\n                          <Pressable\n                            onPress={() =>\n                              router.push(\n                                '/workout/completion'\n                              )\n                            }\n                            style={({ pressed }) => [\n                              styles.validateButton,\n                              pressed &&\n                                styles.validateButtonPressed,\n                            ]}\n                          >\n                            <Ionicons\n                              name=\"arrow-forward-circle-outline\"\n                              size={20}\n                              color={colors.brandWhite}\n                            />\n                            <Text\n                              style={styles.validateButtonText}\n                            >\n                              RETOURNER À LA FIN DE SÉANCE\n                            </Text>\n                          </Pressable>\n                        ) : (\n                          <Pressable\n                            onPress={() =>\n                              validateBlock(\n                                block.id\n                              )\n                            }\n                            disabled={\n                              !canValidate ||\n                              block.validated\n                            }\n                            style={({ pressed }) => [\n                              styles.validateButton,\n                              block.validated &&\n                                styles.validateButtonDone,\n                              !canValidate &&\n                                !block.validated &&\n                                styles.validateButtonDisabled,\n                              pressed &&\n                                canValidate &&\n                                !block.validated &&\n                                styles.validateButtonPressed,\n                            ]}\n                          >\n                            {block.validated ? (\n                              <>\n                                <Ionicons\n                                  name=\"checkmark-circle\"\n                                  size={20}\n                                  color={\n                                    colors.primaryLight\n                                  }\n                                />\n                                <Text\n                                  style={styles.validateButtonDoneText}\n                                >\n                                  BLOC VALIDÉ\n                                </Text>\n                              </>\n                            ) : (\n                              <Text\n                                style={[\n                                  styles.validateButtonText,\n                                  !canValidate &&\n                                    styles.validateButtonTextDisabled,\n                                ]}\n                              >\n                                {block.id ===\n                                  'warmup' &&\n                                  'TERMINER LE WARM-UP'}\n                                {block.id ===\n                                  'tabata' &&\n                                  'TERMINER LE TABATA'}\n                                {block.id ===\n                                  'skill' &&\n                                  'TERMINER LE SKILL'}\n                                {block.id ===\n                                  'wod' &&\n                                  'TERMINER LA SÉANCE'}\n                              </Text>\n                            )}\n                          </Pressable>\n                        )}`;

text = text.replace(oldBlock, newBlock);
fs.writeFileSync(sessionPath, text.replace(/\n/g, eol), 'utf8');

console.log('SESSION RETURN TO COMPLETION PATCH APPLIED SUCCESSFULLY');
console.log('Modified: app/workout/session.js');
console.log('Behavior: after entering completion and going back to review the session, the validated WOD shows an active RETOURNER A LA FIN DE SEANCE button.');
console.log('No workout data or validation state is reset.');
